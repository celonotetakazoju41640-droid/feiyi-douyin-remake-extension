# 2026-07-16 清空记录重置摘要视角

## 用户原始需求摘要

用户要把整条主链路继续收完整，重新开始时也应该回到最干净、最稳定的初始态，不能留下上一轮项目的视角残留。

## AI 对需求的理解

当前已经补了不少默认落点：

- 新生成项目后默认回到 `项目摘要`
- 切换项目时默认回到 `项目摘要`
- 导入 JSON 后默认回到 `项目摘要`

但 `清空本地记录` 这一步还缺一个显式重置：

- 项目数据虽然清空了
- 右侧面板内容也清掉了
- 但 `activeDetailTab` 本身没有被明确重置回 `summary`

这会让下一次重新生成或导入时，默认视角稳定性继续依赖旧状态是否正好被覆盖。

## 当前假设

- 本轮不改清空逻辑本身，只补视角重置。
- 清空本地记录后，应显式把详情标签状态重置回 `summary`。
- 不需要改删除单个项目的当前行为，除非验证发现同类残留。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/workspace.html`
- 不改 `src/workspace.css`
- 不改 `src/remake-core.js`
- 不改项目删除、导入、提交、故事版、导出等业务逻辑
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 先补测试，锁定清空本地记录后会重置 `activeDetailTab`。
2. 修改 `src/workspace.js`，在 `clearProjects()` 里显式回到 `summary`。
3. 同步 `README.md`，补充清空后的初始态规则。
4. 跑相关检查命令。
5. 只提交本轮相关文件。

## 验收标准

- 清空本地记录后，详情标签状态显式回到 `summary`。
- 后续重新生成或导入项目时，不会再受清空前最后停留标签影响。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`

## 风险和需要注意的事项

- 这轮只补清空后的视角重置，不改项目数据清空逻辑。
- 不能影响现有清空后的空状态展示。

## 执行后的实际结果

- 已修改 `src/workspace.js`：`clearProjects()` 里现在会显式把 `activeDetailTab` 重置为 `summary`。
- 已补 `tests/extension-shell.test.mjs`：锁定清空本地记录后也会回到 `项目摘要` 标签状态。
- 已同步 `README.md`，补充清空记录后的默认视角规则。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
