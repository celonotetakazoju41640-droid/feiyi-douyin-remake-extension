# 2026-07-13 TikTok 提示词英文修复

## 用户原始需求摘要

用户确认修复当前生成链路里的语言问题：TikTok 生成结果正文必须是英文，抖音继续保持中文。

## 我对本次任务的理解

这不是 UI 问题，也不是提交链路问题。  
当前平台切换、模板切换、口播语言元数据已经联动成功，TikTok 也已经切成英文骨架。
本轮不是继续修“有没有切英文”，而是继续把 TikTok 英文提示词从“结构说明型骨架”往“更像海外投手 / 产品经理会直接拿去跑的 native 表达”收一轮。

## 当前假设

- `生成 -> 历史记录 -> 提交生成` 主链路已经可用
- 问题主要在 `src/remake-core.js` 的 TikTok 英文提示词拼装逻辑
- 现有测试入口 `tests/remake-core.test.mjs` 可作为本轮最小验证入口
- `src/workspace.js` 预计只需要做语法回归检查，不需要改 UI

## 计划修改范围

- `tests/remake-core.test.mjs`
- `src/remake-core.js`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 UI 结构
- 不改本地服务接口
- 不改蒸馏管理逻辑
- 不改深蒸馏分析逻辑
- 不改抖音中文提示词表达
- 不顺手调整历史记录页和生成页样式
- 不清理 `scripts/runtime/` 和 `src/workspace-flow-test.html`

## 执行步骤

1. 先补一条更严格的失败测试，证明当前 TikTok 英文提示词仍然偏“结构说明型”
2. 只改 `src/remake-core.js` 里的 TikTok 英文文案模板，重点收 batch prompt、shot prompt、variant summary、deep-distill summary 和默认英文语气
3. 跑最小测试和语法检查，确认 TikTok 英文断言通过、抖音中文断言不回归
4. 同步 README 和计划结果
5. 完成 Git 提交和推送闭环

## 验收标准

- TikTok 生成出的批量提示词正文继续为英文，且英文表达更自然，不再明显像说明书骨架
- TikTok 镜头提示词、候选版本摘要和深蒸馏英文摘要同步变得更像海外投手表达
- 抖音生成出的批量提示词正文继续为中文
- `node --test tests/remake-core.test.mjs` 通过
- `node --check src/remake-core.js` 通过
- `node --check src/workspace.js` 通过
- `git diff --check` 通过
- 真实页面复验中，`生成 -> 历史记录` 仍然正常
- 真实页面复验中，`提交生成` 仍然正常
- 本轮 Git commit 和 push 完成

## 风险和需要用户确认的事项

- 本轮只收 TikTok 英文自然度，不扩大到 UI、流程或抖音链路
- 英文如果润色过头，可能会削弱原本便于结构化生成的约束感，所以本轮只改语气和措辞，不改核心信息结构

## 执行后的实际结果

- 已先补一条更严格的英文自然度测试，明确卡住当前英文还偏“结构说明型”的问题
- 已只修改 `src/remake-core.js` 里的 TikTok 英文提示词模板，没有扩到 UI、流程或抖音中文链路
- 已把这些 TikTok 英文段落继续润色成更接近海外投手表达的口吻：
  - 批量任务 prompt 开头和字段语气
  - 镜头级 video shot prompt
  - 候选版本 summary 和 shot 指令
  - deep-distill 英文摘要
- README 已同步本轮状态
- 已跑通过：
  - `node --test tests/remake-core.test.mjs`
  - `node --check src/remake-core.js`
  - `node --check src/workspace.js`
  - `git diff --check`
- 尝试过直接复用当前 Chrome 已打开的扩展工作台做真实页面 TikTok 复验，但扩展页 `chrome-extension://.../workspace.html` 被浏览器自动化安全策略拦截，当前无法合规读取或操作，所以这轮没法在自动化里补这一步

## 验证方式

- `node --test tests/remake-core.test.mjs`
- `node --check src/workspace.js`
- `node --check src/remake-core.js`
- `git diff --check`
- 已尝试连接当前 Chrome 扩展工作台做真实页面复验，但被 `chrome-extension://` 页面安全策略拦截
