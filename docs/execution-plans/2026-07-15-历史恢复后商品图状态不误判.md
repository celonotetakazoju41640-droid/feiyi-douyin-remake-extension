# 2026-07-15 历史恢复后商品图状态不误判

## 用户原始需求摘要

用户要的是一条龙主链路真正可持续使用，不是项目都已经生成了，刷新或切回历史后页面还把状态提示说成“没上传商品图”。

## AI 对需求的理解

代码审计发现很多状态提示只看 `nodes.productImages.files`：

- 资产状态
- 生成流程摘要
- 主提示文案

但浏览器刷新后不会保留文件输入，所以即使当前项目里已经记录了：

- `clipcatConfig.productImageCount`
- 已生成项目

页面仍会把用户误显示成：

- `商品图：未上传`
- `待上传`
- `再上传商品图就能生成`

## 当前假设

- 本轮只修状态层误判。
- 不改真实生成逻辑，不让生成按钮在没有新文件时误放开。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/remake-core.js`
- 不改故事版逻辑
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定历史恢复后商品图状态仍能识别已上传数量。
2. 修改 `src/workspace.js`，新增“已知商品图数量”判断。
3. 状态提示、资产状态、流程摘要改为优先参考 `clipcatConfig.productImageCount`。
4. 生成按钮继续只认当前会话里新上传的文件，避免误触发重新生成。
5. 同步 `README.md`。
6. 跑检查命令并完成 Git 闭环。

## 验收标准

- 历史恢复或刷新后，只要当前项目里已记录 `productImageCount`，状态层不会误显示成“未上传商品图”。
- 没有新文件时，生成按钮仍保持谨慎，不会误放开。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`

## 风险和需要注意的事项

- 不能为了修状态提示，把真正的重新生成门槛降掉。
- 状态层和可操作层要分开判断。

## 执行后的实际结果

- 已补 `tests/extension-shell.test.mjs`：要求存在 `getKnownProductImageCount()`，并验证历史恢复后状态提示会继续识别已上传商品图数量。
- 已修改 `src/workspace.js`：
  - 新增 `getKnownProductImageCount()`
  - 资产状态、流程摘要、主提示文案优先参考已知商品图数量
  - 生成按钮仍只认当前会话里新上传的图片文件

## 验证命令或验证方式

- 已执行 `node --check src/workspace.js`
- 已执行 `node --test tests/extension-shell.test.mjs`
- 已执行 `git diff --check`
