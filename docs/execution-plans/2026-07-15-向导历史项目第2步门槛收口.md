# 2026-07-15 向导历史项目第 2 步门槛收口

## 用户原始需求摘要

用户要继续按真实用户视角，把“选择蒸馏模型 -> 一键传图 -> AI 自动识别 -> 生成提示词 -> 一键生成叙事图 -> 复制提示词图片一条龙”这条主链路收完整。

## AI 对需求的理解

当前主链路大部分已经接通，但历史恢复项目在向导模式里还有一个体验断点：

- 项目本身已经记录过商品图数量
- 用户只是想继续看第 3 / 4 步结构或接着处理历史项目
- 结果向导第 2 步仍然按“当前文件输入为空”拦住

这会让用户误以为项目本身没有商品图状态，属于主链路状态门槛还没完全统一。

## 当前假设

- 历史项目里的 `clipcatConfig.productImageCount` 已能代表“当前项目已知有商品图”。
- 正式重新生成新一轮结果时，仍然必须要求 fresh upload，不能放松真实生成门槛。
- 这轮只修“浏览 / 继续看步骤”门槛，不改真正生成门槛。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`
- 不改 TikTok / 抖音提示词正文算法
- 不扩新功能

## 执行步骤

1. 让向导第 2 步判断已知商品图状态，而不是只看当前文件输入。
2. 保持第 3 步正式生成时仍然要求 fresh upload。
3. 补壳测试，锁定这个历史恢复场景。
4. 跑相关验证命令。
5. 如果验证通过，继续纳入 Git 闭环。

## 验收标准

- 历史恢复项目如果已知有商品图，可从向导第 2 步继续看后续步骤。
- 真正点击生成时，仍然要求重新上传当前轮要用的商品图。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`

## 风险和需要注意的事项

- 不能把“可继续看步骤”和“可直接生成”混为一谈。
- 不能因为放松第 2 步浏览门槛，导致第 3 步重新生成也绕过 fresh upload 要求。

## 执行后的实际结果

- 已把向导第 2 步门槛改成优先看 `getKnownProductImageCount()`。
- 历史恢复项目如果本身记录过商品图数量，现在可以继续看后续步骤。
- 第 3 步真正生成时，仍然保留“必须 fresh upload”的硬门槛。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
