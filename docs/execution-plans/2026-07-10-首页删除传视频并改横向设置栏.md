# 2026-07-10 首页删除传视频并改横向设置栏

## 用户原始需求摘要

用户要求：

- 把首页这个“传视频”整块删掉
- 把“中文视频设置”这一栏做成横条
- 不要再上下堆很多块

## 我对本次任务的理解

这轮只继续收首页生成区：

- 删除首页可见的视频上传区
- 保留底层视频上传节点到隐藏区，避免现有脚本失效
- 把设置区改成更扁平的横向一条

## 当前假设

- 用户要删的是首页可见视频上传区，不是要删掉底层参考视频能力
- 这轮只动 `src/workspace.html`、`src/workspace.css`、README 和执行计划
- 生成逻辑不用改

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- `src/workspace.js`
- `src/remake-core.js`
- 历史记录
- 蒸馏管理
- 外部服务协议

## 执行步骤

1. 删掉首页可见的视频上传块
2. 把视频上传相关节点放到隐藏区
3. 把中文视频设置改成横向设置栏
4. 跑检查并核对真实页面

## 验收标准

- 首页不再显示“请上传对标短剧视频”
- 中文视频设置在桌面端是横向条状布局
- 现有脚本不报错
- `git diff --check` 通过

## 执行后的实际结果

- 首页可见的传视频整块已经删除
- 视频上传相关节点已移到隐藏区，继续给现有脚本复用
- 中文视频设置当前已改成桌面端横向设置栏
- 首页上方现在只保留商品图上传和横向设置区，整体更短更平

## 验证记录

- `git diff --check`
- 真实页面截图核对：
  - 首页不再显示传视频块
  - 中文视频设置已变成横向一条
