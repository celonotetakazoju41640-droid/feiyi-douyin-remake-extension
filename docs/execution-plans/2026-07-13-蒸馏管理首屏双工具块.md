# 2026-07-13 蒸馏管理首屏双工具块

## 用户原始需求摘要

用户要求继续，并要求继续按开发 skill 的标准流程推进。本轮聚焦 `蒸馏管理` 首屏。

## 本次目标

把 `蒸馏管理` 首屏收成两个明确工具块：`模型与主页提炼`、`视频深蒸馏`。

## 当前假设

- 用户不希望首屏出现太多同级信息
- 模型基础字段、深层规则和样本筛片可以继续后置在折叠区
- 本轮主要解决的是层级和排版，不是功能缺失

## 修改范围

- `src/workspace.html`
- `src/workspace.css`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改主页提炼逻辑
- 不改视频深蒸馏逻辑
- 不改模型字段内容
- 不改样本筛片和深层规则功能

## 执行步骤

1. 收首屏结构为双工具块
2. 压缩模型与主页提炼块的层级
3. 压缩视频深蒸馏块的说明和操作区
4. 保持折叠区后置
5. 更新 README
6. 跑检查

## 验收标准

- 首屏一眼只看到两个主工具块
- 主页提炼和视频深蒸馏不再互相抢层级
- 折叠区还在，但不压首屏
- `node --check src/workspace.js` 通过
- `git diff --check` 通过

## 执行后的实际结果

- 已把蒸馏管理首屏收成 `模型与主页提炼`、`视频深蒸馏` 两个主工具块
- 已把主页提炼和视频深蒸馏的提示与操作拆开，减少互相抢层级
- 已保留模型字段、深层规则和样本筛片折叠后置
- 已同步更新 README 的蒸馏管理说明
