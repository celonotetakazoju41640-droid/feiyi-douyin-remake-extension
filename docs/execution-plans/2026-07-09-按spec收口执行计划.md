# 2026-07-09 按 spec 收口执行计划

## 当前状态

- 已有权威 spec：`docs/execution-plans/2026-07-09-向导式UI重构-spec.md`
- 当前代码已经切到向导式骨架，但样式、说明文档和细节交互还没完全收口
- 当前真实校验已通过：
  - `node --check src/workspace.js`
  - `node --test tests/extension-shell.test.mjs`
  - `node --test tests/remake-core.test.mjs`
  - `git diff --check`

## 用户原始需求摘要

用户要求不要再靠感觉改 UI，要严格按已经确认的 spec 收口，做成更像“创建任务”的成熟工具：

- 左侧步骤栏
- 右侧当前一步
- 底部上一步 / 下一步
- 页面更短、更收、更像 C 端工具

## 我对本次任务的理解

本次不是继续发散做新方案，而是把现在这版向导式前台真正收口到 spec：

- 首页主流程只保留 4 步
- 复杂能力退到 `官方高级后台`
- README 和执行文档也同步到向导式口径

## 当前假设

- 现有 spec 是本轮权威依据
- 服务端、计费、真实视频生成、本地批量服务协议都不在本轮范围
- 当前测试集足够覆盖这轮结构性改动的基本回归

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 相关执行文档中仍描述旧“大卡片首页”的文件

## 明确不修改的范围

- `src/remake-core.js`
- 抖音 / TikTok 抓取和蒸馏算法
- 真实视频提交服务和 API 密钥处理
- 付费、额度、权限和后端逻辑

## 执行步骤

1. 对照 spec 检查当前向导 UI 还不一致的地方
2. 收口首页 HTML、CSS、JS，只保留主流程需要的信息
3. 把 `官方高级后台` 继续降级成次级入口，避免抢主流程
4. 同步 README 和相关执行文档，去掉旧“大卡片极速前台”口径
5. 重新跑校验并检查差异

## 验收标准

- 首页结构符合 4 步向导 spec
- 首屏不会再同时暴露多段后台式内容
- 第二步上传区明显缩小，不再是海报级大块
- 第四步是“结果与提交”完成页，不是后台结果堆叠页
- `官方高级后台` 不进入主步骤流
- 以下命令重新通过：
  - `node --check src/workspace.js`
  - `node --test tests/extension-shell.test.mjs`
  - `node --test tests/remake-core.test.mjs`
  - `git diff --check`

## 风险和注意事项

- `src/workspace.js` 里仍保留不少旧节点引用，本轮只能处理会影响当前向导的部分，不能顺手重构整文件
- 如果文档同步范围继续扩大，需要先确认是否还属于本轮“向导式 UI 收口”
- 本轮先以真实源码和测试为准，不拿旧扩展缓存页当证据

## 执行后实际结果

- 已修正向导页的核心结构问题：`hidden` 步骤面板不再被 CSS 覆盖，首页真正恢复成“一次只显示当前一步”的 4 步向导。
- 第一步说明已按 spec 收口，不再错误显示“生成条数”属于基础信息。
- 第二步上传区已补向导专属样式覆盖，避免继续被旧首页样式干扰。
- 已继续按参考视频的工作台逻辑做一轮视觉收口：首页改成 `左步骤 + 中间当前任务 + 右侧操作坞`，主按钮权重明显提升。
- README 和上一阶段执行文档已同步标注：当前权威口径是向导式创建任务，不再是“大卡片极速前台”。
- 本轮没有改服务端、计费、权限、真实生成和抓取算法。

## 验证记录

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `node --test tests/remake-core.test.mjs`
- `git diff --check`
- 本地页面预览已复查：
  - 第一步只显示基础信息
  - 点击左侧未来步骤会切换并给轻提示
  - “下一步” 可以进入第二步
  - 桌面宽度下已呈现工作台式三栏结构
