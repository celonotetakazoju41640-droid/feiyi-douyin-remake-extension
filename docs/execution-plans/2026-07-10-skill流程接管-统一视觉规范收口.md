# 2026-07-10 skill 流程接管：统一视觉规范收口

## 用户原始需求摘要

用户要求继续按专业分工推进，这一轮专门做统一视觉，不再继续零碎调结构。

## 我对本次任务的理解

上一轮已经把三个主栏目结构拉顺了。这一轮只做视觉规范统一，让 `生成 / 历史记录 / 蒸馏管理` 看起来像同一套成熟产品，而不是三个分别调出来的页面。

## 当前假设

- 当前主要问题不在功能，而在视觉语言还不够统一
- 标题、副标题、主次按钮、空状态、折叠区、卡片层级是最值得统一的 6 个点
- 这轮只需要改 `src/workspace.css`，不需要改业务逻辑

## 本次目标

- 三页标题区风格统一
- 主按钮和次按钮的层级统一
- 卡片边框、圆角、阴影统一
- 折叠区 summary 的质感统一
- 空状态卡片风格统一
- 状态 badge 和统计 badge 统一

## 计划修改范围

- `src/workspace.css`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- `src/workspace.html`
- `src/workspace.js`
- 业务逻辑
- 数据结构

## 执行步骤

1. 统一标题区、区块标题和副标题样式
2. 统一按钮、badge、卡片和折叠区样式
3. 做真实页面截图核对三页首屏
4. 更新 README 和执行文档，再做检查和提交

## 验收标准

- 三个主页面首屏视觉语言统一
- 主按钮和次按钮的轻重关系更明确
- 折叠区和空状态看起来属于同一套组件体系
- `git diff --check` 通过

## 执行后的实际结果

- 三页标题区的字号、字重和副标题节奏已经统一
- 主按钮和次按钮的圆角、阴影、轻重关系已经统一
- 卡片边框、圆角和阴影已经统一成一套规则
- 折叠区 summary、空状态卡片、badge 都已经统一进同一套视觉语言
- 三个主页面首屏现在看起来属于同一套产品，而不是三段分别调出来的页面

## 验证方式

- 打开本地 `src/workspace.html`
- 分别核对 `生成 / 历史记录 / 蒸馏管理` 三页首屏截图
- 人工检查标题区、按钮、折叠区和空状态的一致性
- 补跑：
  - `git diff --check`
  - `git status --short --branch`
