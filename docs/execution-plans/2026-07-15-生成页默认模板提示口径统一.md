# 2026-07-15 生成页默认模板提示口径统一

## 用户原始需求摘要

用户要继续把一条龙主链路收完整，重点是前台体验要更顺，不要让用户在每一步都猜当前状态。

## AI 对需求的理解

真实页面验收时已经确认：

- 页面默认已经有可用蒸馏模板
- `主流程状态` 会显示“再上传商品图就能生成”
- 但顶部 `actionFeedback` 还显示“先上传商品图”

这会让用户误以为还差“选模板”这一步，属于主链路提示口径没完全统一。

## 当前假设

- 本轮只修默认模板已存在场景下的生成页反馈口径。
- 不触发真实外部服务，不做真实生成动作。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改故事版逻辑
- 不改真实生成链路
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定“默认模板已存在时应提示再上传商品图”。
2. 修改 `src/workspace.js` 的反馈分支。
3. 同步 `README.md`。
4. 跑检查命令并完成 Git 闭环。

## 验收标准

- 默认模板已存在、但还没上传商品图时，生成页提示和主流程状态都指向“再上传商品图就能生成”。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 不能误伤“还没选模板”的场景，那个场景仍然要明确提示先选蒸馏模型。

## 执行后的实际结果

- 已修改 `src/workspace.js`：默认模板已存在时，只要还没上传商品图，顶部提示统一改成“再上传商品图就能生成”，不再退回“先上传商品图”。
- 已补测试，防止 `!hasImages && !hasPrompt` 分支再次回到旧口径。
- 已同步 `README.md`，补充这条前台提示规则。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
