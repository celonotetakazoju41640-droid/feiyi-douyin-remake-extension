# 2026-07-08 抖音蒸馏第二层执行计划

## 用户原始需求摘要

继续把“飞蚁抖音复刻”Chrome 扩展做完，重点提升抖音公开主页蒸馏的可用性和稳定性，并保证页面不要错位拉胯。

## AI 对需求的理解

这轮不是重做整个扩展，而是在已经接入“抖音公开主页蒸馏第一版”的基础上，继续补第二层能力，让抖音主页样本不只停留在“能抓到几个链接”，而是尽量补到更可靠的标题、封面、时长和互动数据，并同步核对页面和文档。

## 当前假设

- 当前主项目目录是`/Users/da/Desktop/飞蚁抖音复刻-Chrome扩展`。
- 第一版抖音公开主页蒸馏已经提交到`main`分支，当前工作区干净。
- 抖音公开页会存在加载波动，所以这轮目标是“提升稳定性”，不是承诺百分之百抓全。
- 用户允许继续执行，不需要再等待额外口头确认。

## 计划修改范围

- `src/workspace.js`
- `src/workspace.html`
- `README.md`
- `ROADMAP.md`
- `tests/remake-core.test.mjs`（仅当本轮新增了可测的纯逻辑时）
- `docs/execution-plans/2026-07-08-抖音蒸馏第二层.md`

## 明确不修改的范围

- 不接真实视频生成 API。
- 不在前端保存任何 API Key。
- 不做登录态抓取、私有内容抓取。
- 不大改 TikTok 现有链路。
- 不顺手重构整个工作台样式体系。

## 执行步骤

1. 核对当前仓库状态、最近提交和抖音蒸馏现状。
2. 分析抖音公开主页当前能拿到的真实信息来源，优先补页面脚本数据和 DOM 兜底解析。
3. 在`src/workspace.js`里增强抖音样本提取，提升标题、封面、时长和互动数据命中率。
4. 同步更新工作台文案和说明文档，避免代码和 README 口径不一致。
5. 跑语法和测试检查，必要时再打开本地页面做 UI 核查。
6. 走 Git 提交和推送闭环。

## 验收标准

- 抖音主页蒸馏仍可正常触发，不回退第一版入口能力。
- 抖音样本结果里，标题/封面/时长/互动数据至少有一部分明显比第一版更完整。
- `node --check src/workspace.js`通过。
- `node --check src/remake-core.js`通过。
- `node --test tests/remake-core.test.mjs`通过。
- README 和路线图对“抖音权限、当前能力、当前边界”的描述与代码一致。
- Git 有新的相关提交并成功推到远端。

## 风险和需要注意的事项

- 抖音公开页本身可能间歇性返回异常页，这会影响样本数量稳定性。
- 这轮不触发真实外部生成，不产生真实视频成本。
- 如果实现过程中发现必须扩大到后台服务或真实接口，会先停下来重新说明范围。

## 执行后的实际结果

- 已在`src/workspace.js`把抖音主页蒸馏从“主要靠视频锚点兜底”升级为“优先解析页面脚本数据，再合并 DOM 卡片结果兜底”。
- 已增加抖音扫描重试和结果质量打分，优先保留更完整的一轮结果。
- 已补充卡片级提取：在真实可见卡片存在时，可直接抓到封面图、点赞数和去重后的标题。
- 已进一步把卡片文案提取从泛匹配收紧到真实标题节点优先，并补了背景图封面兜底。
- 已增加详情页补数：会对前几条缺失样本尝试补抓公开视频详情页，补时长、评论和分享。
- 详情页补数当前已验证可直接命中 `data-e2e` 动作节点，不再主要依赖正文里的数字顺序猜测。
- 已把扫描完成提示改成可见统计，直接告诉用户抓到多少条有封面、多少条带播放数据、多少条带时长。
- 已同步更新`src/workspace.html`、`README.md`、`ROADMAP.md`，让工作台说明、权限说明和当前阶段描述与代码一致。
- 已检查本地页面，当前界面没有因为本轮改动出现明显错位；控制台也没有新增报错。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --check src/remake-core.js`
- `node --check src/popup.js`
- `node --test tests/remake-core.test.mjs`
- 本地打开`http://127.0.0.1:8765/src/workspace.html`做页面核查
- 实测真实抖音公开主页：
  - `曹先生（手机摄影）`
  - `新华社三农`
- 切到抖音模式后，确认：
  - 按钮文案为“扫描抖音主页并蒸馏”
  - 状态为“未扫描”
  - 主页占位符为`https://www.douyin.com/user/xxxx`
  - 蒸馏说明文案已更新为“优先解析页面脚本数据，再用页面卡片兜底”
