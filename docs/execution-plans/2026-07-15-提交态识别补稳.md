# 2026-07-15 提交态识别补稳

## 用户原始需求摘要

用户要的是整条主链路状态说真话，不能因为内部状态值稍微变化，前台就误判成还没提交。

## AI 对需求的理解

当前历史卡片和结果区的提交态判断只认一种状态：

- `queued`

如果本地服务返回的是：

- `submitted`
- `running`
- `processing`

或者虽然状态值变化了，但项目里已经有 `batchId`，前台仍可能误回到 `可提交`。

这会让已提交项目再次看起来像未提交项目。

## 当前假设

- 本轮只补提交态识别规则，不改真实提交链路。
- 不触发真实外部服务。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改故事版生成逻辑
- 不改真实生成链路
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定 `batchId` 或多种处理中状态都应视为已提交。
2. 修改 `src/workspace.js` 的历史状态判断。
3. 同步 `README.md`。
4. 跑检查命令并完成 Git 闭环。

## 验收标准

- 只要项目里已有 `batchId`，或任务状态是 `queued / submitted / running / processing`，前台都识别为 `已提交`。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 不能把真正未提交的草稿任务误判成已提交。
- 规则要足够白话且稳定，不要再绑死单一状态值。

## 执行后的实际结果

- 已修改 `src/workspace.js`：历史状态判断现在会同时识别 `batchId` 和 `queued / submitted / running / processing` 这几类提交中状态。
- 已补测试，要求代码明确包含 `submittedStatuses` 和 `batchId` 判断。
- 已同步 `README.md`，补充当前提交态识别规则。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
