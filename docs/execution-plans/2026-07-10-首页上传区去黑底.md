# 2026-07-10 首页上传区去黑底

## 用户原始需求摘要

用户要求把首页上传区的黑色背景调回去，不要黑色底。

## 我对本次任务的理解

这轮只处理首页生成区上传板的颜色，不改结构，不改控件位置，不改生成逻辑。

## 当前假设

- 用户认可当前“上传区在上，参数区在下”的结构
- 本轮只改 `src/workspace.css`
- 上传区需要回到和整页一致的暖色浅底

## 计划修改范围

- `src/workspace.css`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- `src/workspace.html`
- `src/workspace.js`
- `src/remake-core.js`
- 历史记录和蒸馏管理
- 生成逻辑和任务协议

## 执行步骤

1. 把上传区外层卡片从深色改回暖色浅底
2. 把上传格子和预览底色同步改浅
3. 调整上传区标题和辅助文字颜色
4. 抓真实页面截图核对
5. 跑最小检查并提交

## 验收标准

- 首页上传区不再出现黑色背景
- 页面整体仍保持当前暖色方向
- 结构和交互不受影响
- `git diff --check` 通过

## 执行后的实际结果

- 上传区外层卡片已改成暖色浅底
- 两个上传格子和预览底色都已同步去掉黑底
- 上传区标题、辅助文案和徽标颜色已改回浅色页面适配版本

## 验证记录

- `git diff --check`
- 真实页面截图核对：
  - 首页上传区已无黑色背景
