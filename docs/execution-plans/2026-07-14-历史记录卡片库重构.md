# 2026-07-14 历史记录卡片库重构

## 用户原始需求摘要

用户希望把 `历史记录` 做得更像参考 UI：复杂内容放到后台和详情区，首屏先简单明了，不要把结果、按钮、长文案一次性全摊开。

## 我对本次任务的理解

本轮只收 `历史记录` 页的信息结构，不改生成逻辑：

1. 首屏先展示项目卡片库
2. 用户先选项目
3. 再看当前项目详情和操作

重点是把“先详情、再列表”的结构，改成“先列表、再详情”。

## 当前假设

- 用户借鉴的是“卡片库 + 详情工作区”的产品结构，不是照搬视觉风格
- 现有项目数据已经足够支撑卡片列表，不需要新增后端字段
- 复杂工具仍然继续折叠在高级区，不进首屏

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改生成逻辑
- 不改提交生成链路
- 不改蒸馏管理逻辑
- 不改生成页
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 重排历史记录页结构，首屏先显示项目库
2. 把当前结果摘要和主操作收进详情工作区左侧
3. 保留详情标签页，但调整成右侧主内容区
4. 更新项目卡片渲染和空状态文案
5. 更新测试和 README
6. 跑检查并完成 Git 提交上传

## 验收标准

- 历史记录首屏明显更清爽，不再大段文字压前面
- 用户可以先选项目，再看详情和提交操作
- `node --check src/workspace.js` 通过
- `node --test tests/extension-shell.test.mjs` 通过
- `git diff --check` 通过

## 风险和需要用户确认的事项

- 本轮会改历史记录页前台结构，但不改底层结果数据
- `src/workspace.css` 里仍有不属于本轮的未提交改动，提交时只暂存本轮相关内容

## 执行后的实际结果

- 已把历史记录首屏改成先看 `项目库` 卡片，再看当前项目详情
- 已把当前项目摘要和主操作收进左侧工作区
- 已保留右侧详情标签页，继续承接摘要、提示词、批量任务和镜头细节
- 已把项目列表改成更简短的卡片信息，不再首屏直接摊长文案
- 已同步 README 和测试断言
- 本轮未改生成逻辑、提交生成链路和蒸馏逻辑

## 验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
