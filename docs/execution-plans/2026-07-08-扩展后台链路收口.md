# 2026-07-08 扩展后台链路收口执行计划

## 用户原始需求摘要

继续检查并补完“飞蚁抖音复刻”Chrome 扩展，把还没收口的壳子问题处理掉。

## AI 对需求的理解

这轮不是做新的蒸馏能力，而是把扩展壳层的两个硬问题收口：

1. `manifest.json` 没有正式挂上 MV3 后台 service worker，旧消息转发链路写了代码但实际起不来。
2. 旧后台链路里本地服务端口还是 `4318`，和当前主工作台 `4328` 口径不一致。

同时顺手清掉工作台上几个明显没接逻辑、容易误导的草稿控件。

## 当前假设

- 主项目目录是`/Users/da/Desktop/飞蚁抖音复刻-Chrome扩展`。
- 用户已明确允许继续执行。
- 本轮只处理扩展壳层、共享配置和说明文档，不改 TikTok / 抖音蒸馏主逻辑。
- 允许为恢复旧后台链路补最小必要权限，但不会接真实视频生成或密钥。

## 计划修改范围

- `manifest.json`
- `src/background-core.js`
- `src/background.js`
- `src/workspace.js`
- `src/gptsdd-automation-core.js`
- `src/workspace.html`
- `src/workspace.css`
- `README.md`
- `ROADMAP.md`
- `tests/extension-shell.test.mjs`
- `docs/execution-plans/2026-07-08-扩展后台链路收口.md`

## 明确不修改的范围

- 不改 TikTok / 抖音公开主页蒸馏算法。
- 不改复刻包输出结构。
- 不接真实生成 API。
- 不保存任何密钥。
- 不扩到服务端仓库。

## 执行步骤

1. 先写失败测试，锁定后台声明、权限口径和断开控件。
2. 补 `manifest.json` 的 MV3 后台声明和必要权限。
3. 统一本地服务地址，避免 `4318 / 4328` 两套口径继续漂移。
4. 清理工作台里没接逻辑的草稿控件和对应样式。
5. 同步更新 README、路线图和执行计划。
6. 跑测试、语法和 diff 检查，再走 Git 提交推送闭环。

## 验收标准

- `manifest.json` 包含 `background.service_worker = "src/background.js"` 且类型为 `module`。
- 扩展权限口径与代码一致，不再出现后台链路打 `4318`、工作台打 `4328` 的分裂情况。
- 工作台页面不再暴露 `referenceReanalysisButton`、`videoLink`、`aspectRatio`、`copyLanguage` 这些断开的草稿控件。
- `node --test tests/extension-shell.test.mjs` 通过。
- `node --test tests/remake-core.test.mjs` 通过。
- `for f in src/*.js; do node --check "$f"; done` 通过。
- `git diff --check` 通过。

## 风险和需要注意的事项

- 补后台链路后，扩展权限会多出 `storage` 和 `https://vip.gptsdd.com/*`，需要在文档里解释用途。
- 这轮只恢复壳层链路，不承诺 GPTSDD 页面或本地服务端一定在线。
- 不会触发真实视频生成，不产生真实费用。

## 执行后的实际结果

- 已在`manifest.json`补上 MV3 后台 `service_worker`，并增加 `storage` 与 `https://vip.gptsdd.com/*` 的最小必要权限。
- 已新增`src/extension-config.js`，把本地服务地址、启动命令和 GPTSDD 页面地址统一收口，避免 `4318 / 4328` 再次漂移。
- 已把`src/background-core.js`和`src/workspace.js`改为复用同一套共享配置，旧后台链路和当前工作台现在都统一指向本地 `4328` 服务。
- 已移除工作台里没接逻辑的草稿控件：`referenceReanalysisButton`、`videoLink`、`aspectRatio`、`copyLanguage`，并清理对应样式。
- 已补 `tests/extension-shell.test.mjs`，覆盖后台声明、权限口径和断开控件收口。
- 已同步更新`README.md`和`ROADMAP.md`，让权限、后台链路和当前状态描述与代码一致。

## 验证命令或验证方式

- `node --test tests/extension-shell.test.mjs`
- `node --test tests/remake-core.test.mjs`
- `for f in src/*.js; do node --check "$f"; done`
- `git diff --check`
