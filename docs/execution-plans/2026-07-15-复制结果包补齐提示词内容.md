# 2026-07-15 复制结果包补齐提示词内容

## 用户原始需求摘要

用户要的是从模板、传图、识别、生成，到故事版和结果带走都更完整，特别是“复制提示词图片一条龙”不能只带一半内容。

## AI 对需求的理解

代码审计发现当前 `复制结果包` 文本虽然已经不是原始 JSON，但内容仍偏窄，只包含：

- 项目摘要
- 批量视频任务
- 故事版图

还没有把用户真正会拿去复用的提示词内容一起带走，比如：

- 视频提示词
- 关键帧提示词
- 候选版本摘要

这会让“复制结果包”更像任务包，而不是完整交付包。

## 当前假设

- 本轮只补复制结果包文本内容，不改导出文件结构。
- 不触发真实外部服务。

## 计划修改范围

- `src/remake-core.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改故事版生成逻辑
- 不改真实生成链路
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定复制结果包应包含视频提示词、关键帧提示词和候选版本摘要。
2. 修改 `src/remake-core.js` 的 `buildDeliveryPackageText()`。
3. 同步 `README.md`。
4. 跑检查命令并完成 Git 闭环。

## 验收标准

- 复制结果包文本里明确包含视频提示词、关键帧提示词和候选版本摘要。
- 仍保留批量视频任务和故事版图内容。
- `node --check src/remake-core.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 不能把结果包堆成难读的大段原文，仍要保持按区块组织。
- 不要引入完整敏感配置或额外无关内部字段。

## 执行后的实际结果

- 已修改 `src/remake-core.js` 的 `buildDeliveryPackageText()`，当前复制结果包会额外带上：
  - 视频提示词
  - 关键帧提示词
  - 候选版本摘要与分镜提示词
- 已补测试，要求这些区块明确出现在结果包构建逻辑里。
- 已同步 `README.md`，补充复制结果包当前的内容范围。

## 验证命令或验证方式

- `node --check src/remake-core.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
