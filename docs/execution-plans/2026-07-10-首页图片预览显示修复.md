# 2026-07-10 首页图片预览显示修复

## 用户原始需求摘要

用户反馈首页图片显示还有问题，并问当前上下文是否够继续处理。

## 我对本次任务的理解

这轮只修首页上传格里的图片预览显示问题，不改功能范围。

## 当前假设

- 现有生成逻辑是正常的
- 问题主要在上传格 DOM 结构和 CSS 层级
- 这轮只动首页相关样式和必要的前台结构

## 计划修改范围

- `src/workspace.css`
- 本执行计划文档

## 明确不修改的范围

- `src/workspace.html`
- `src/workspace.js`
- `README.md`
- `src/remake-core.js`
- 历史记录
- 蒸馏管理

## 执行步骤

1. 检查上传格图片节点的定位和层级
2. 修正图片在上传格里的显示方式
3. 抓真实页面截图确认不再错位
4. 跑最小检查并提交

## 验收标准

- 首页上传格里的图片预览不再错位或掉到格子外
- 上传格点击和显示都正常
- `git diff --check` 通过

## 执行后的实际结果

- 已修正首页上传区预览图的定位方式
- 产品图和模特图上传后都会固定显示在各自 108x108 的格子里
- 横向设置区不再被模特图预览顶乱

## 验证方式

- 打开本地 `src/workspace.html` 做真实页面检查
- 人工注入产品图和模特图预览，确认两张图都留在上传格内部
- 计划执行后补跑：
  - `git diff --check`
  - `git status --short --branch`
