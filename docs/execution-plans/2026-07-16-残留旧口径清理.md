# 2026-07-16 残留旧口径清理

## 用户原始需求摘要

用户要把“选择蒸馏模型 -> 一键传图 -> AI 自动识别产品卖点场景 -> 生成提示词 -> 一键点击生成叙事图 -> 复制提示词图片一条龙”这条主链路真正收完整，细节也要考虑到位，不能让普通用户还要自己翻译按钮和提示。

## AI 对需求的理解

当前主链路功能已经基本打通，但前台还残留几处旧口径：

- `可直接去跑的任务`
- `提交去跑视频`
- `可以继续处理`

这些说法太抽象，普通用户仍然需要自己脑补成：

- 提交到本地服务
- 继续追踪批次
- 继续故事版或一键带走

## 当前假设

- 本轮不改功能逻辑，只收前台口径。
- 需要统一生成成功反馈、历史空状态/当前状态提示、向导第 4 步描述等位置。
- 文案要尽量直接对应真实动作：提交到本地服务、复制结果包、导出文件、继续追踪批次。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/workspace.html`
- 不改 `src/workspace.css`
- 不改 `src/remake-core.js`
- 不改提交接口和故事版底层逻辑
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 先补测试，锁定这些旧口径要被更直白的前台说法替代。
2. 修改 `src/workspace.js`，统一生成成功反馈、向导描述和历史区状态提示。
3. 同步 `README.md`，补上新口径。
4. 跑相关检查命令。
5. 只提交本轮相关文件。

## 验收标准

- 生成成功后，不再只说“可直接去跑的任务”，而是明确指向本地服务任务。
- 向导第 4 步不再出现“提交去跑视频”这种抽象说法。
- 历史区空态/提示不再只说“继续处理”，而是更具体地说清下一步。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`

## 风险和需要注意的事项

- 这轮只收口前台文案，不改真实链路行为。
- 文案不能暗示前台已经内置视频结果页或自动回填最终视频成片。

## 执行后的实际结果

- 已修改 `src/workspace.js`：生成成功后，顶部反馈改成“当前有 X 条可提交到本地服务的视频任务”，不再只说“可直接去跑的任务”。
- 已修改 `src/workspace.js`：故事版初始状态改成“生成后可直接点‘生成故事版图’开始”，不再只说“可直接开始”。
- 已修改 `src/workspace.js`：历史区兜底提示改成“先看摘要 / 按建议继续下一步”，不再只说“继续处理”。
- 已修改 `src/workspace.js`：向导第 4 步描述改成“提交到本地服务继续跑视频任务”，不再出现“提交去跑视频”。
- 已补 `tests/extension-shell.test.mjs`：锁定上述新口径，并明确断言旧抽象说法不再出现。
- 已同步 `README.md`，补充生成成功反馈和向导第 4 步的新白话口径。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
