# 2026-07-08 平台默认提示词语言收口执行计划

## 用户原始需求摘要

TikTok 的提示词默认英文，抖音的提示词默认中文。

## AI 对需求的理解

这轮是把平台默认语言口径彻底落到代码里，而不是只在个别预置模板里碰巧生效。需要保证：

- TikTok 模板默认英文
- 抖音模板默认中文
- 工作台切平台时，提示词语言选择能跟着平台一起切换

## 当前假设

- 主项目目录是`/Users/da/Desktop/飞蚁抖音复刻-Chrome扩展`。
- 用户允许直接继续执行。
- 这轮只改默认语言口径，不改蒸馏算法和输出结构。

## 计划修改范围

- `src/remake-core.js`
- `src/workspace.js`
- `tests/remake-core.test.mjs`
- `docs/execution-plans/2026-07-08-平台默认提示词语言收口.md`

## 明确不修改的范围

- 不改 TikTok / 抖音主页抓取逻辑。
- 不改批量任务结构。
- 不改真实视频生成。
- 不改 UI 布局样式。

## 执行步骤

1. 先补失败测试，锁定 TikTok / 抖音默认语言口径。
2. 修核心模板归一化的默认语言规则。
3. 修工作台切平台时的语言联动。
4. 跑测试、语法和浏览器快速验证。
5. 走 Git 提交推送闭环。

## 验收标准

- `normalizeAccountTemplate` 在未显式传语言时：
  - TikTok 返回 `英文`
  - 抖音返回 `中文`
- 工作台平台切换后，Clipcat / 提示词语言会对应切到该平台默认值。
- `node --test tests/remake-core.test.mjs` 通过。
- `node --check src/remake-core.js` 通过。
- `node --check src/workspace.js` 通过。

## 风险和需要注意的事项

- 这轮只收口默认值，不阻止用户后续手动改语言。
- 不触发真实外部服务，不产生费用。

## 执行后的实际结果

- 已在`src/remake-core.js`补上按平台决定默认提示词语言的规则：
  - TikTok 默认 `英文`
  - 抖音默认 `中文`
- 已在`src/workspace.js`补上工作台平台切换联动，切模板平台时会同步把参考平台和提示词语言切到对应默认值。
- 已在`tests/remake-core.test.mjs`增加默认语言回归测试，覆盖 TikTok / 抖音两套平台口径。
- 已用浏览器实测本地工作台：
  - TikTok 初始状态显示 `英文`
  - 切到抖音后显示 `中文`

## 验证命令或验证方式

- `node --test tests/remake-core.test.mjs`
- `node --check src/remake-core.js`
- `node --check src/workspace.js`
