# 2026-07-16 批次追踪入口提示收口

## 用户原始需求摘要

用户要的是一条龙主链路，不只是提交成功，还要知道提交成功后下一步去哪里追踪结果。

## AI 对需求的理解

当前前台已经会显示：

- `已提交到本地服务`
- `当前批次号`
- 提交后的下一步建议

但还缺一句最关键的话：这个批次号到底拿去哪里继续查。

## 当前假设

- 提交逻辑本身已经成立，缺的是批次追踪入口提示。
- 最小可行修复是在批次号旁边和下一步建议里明确写出：按当前批次号去本地服务 / 外部视频工具继续追踪。
- 本轮不改本地服务接口。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/workspace.html`
- 不改 `src/remake-core.js`
- 不改 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 先补壳测试，锁定批次号旁边需要出现追踪入口提示。
2. 修改 `src/workspace.js`，补强批次追踪提示。
3. 同步 `README.md`，写清批次号的使用去向。
4. 跑相关检查命令。
5. 只提交本轮相关文件并 push。

## 验收标准

- 用户能直接看懂批次号该拿去哪里继续查。
- 不影响现有提交、故事版和带走逻辑。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 这轮只补提示，不新增查询接口。
- 文案不能暗示前台已经内置了批次结果页。

## 执行后的实际结果

- 已修改 `src/workspace.js`：批次号旁边现在会明确提示“按当前批次号去本地服务或外部视频工具继续追踪”。
- 已修改 `src/workspace.js`：提交后下一步建议也同步补上批次追踪入口说明。
- 已补 `tests/extension-shell.test.mjs`：锁定批次追踪入口提示文案。
- 已同步 `README.md`，补充批次号的实际使用去向。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
