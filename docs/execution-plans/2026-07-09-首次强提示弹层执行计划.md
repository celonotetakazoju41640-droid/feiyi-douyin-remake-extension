# 2026-07-09 首次强提示弹层执行计划

## 用户原始需求摘要

用户已经确认继续参考 `Pai` 的成熟站点体验，优先补它最值钱的一层：

- 不是继续堆字段
- 不是只改颜色
- 而是在正式创建任务前，先用一层强提示把关键操作规则说清楚

用户当前已明确选择继续做这个方向。

## 我对本次任务的理解

本次要补的是一个 `首次进入先提示、之后可随时再看` 的规则面板。

这层提示不是教程页，也不是营销弹窗，而是直接减少失败率的操作提醒，重点告诉用户：

- 什么时候必须传产品图
- 模特图什么时候才需要
- 主页参考什么时候有效
- 抖音和 TikTok 的语言如何自动切
- 结果生成后为什么还要去视频工具继续提交
- 如果已有脚本，怎么避免系统误改

## 当前假设

- 当前仓库已经完成 4 步向导工作台，本次只是在这个基础上补一层规则前置
- 不改真实生成链路，不改计费，不改权限，不改主页蒸馏算法
- 当前前台使用 `localStorage` 保存项目、模板和筛片状态，因此首次提示的“已看过”状态也沿用本地保存

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`

## 明确不修改的范围

- `src/remake-core.js`
- TikTok / 抖音主页抓取与蒸馏逻辑
- 本地 `4328` 服务协议
- 任何真实外部视频生成调用
- 权限、付费、额度和后台逻辑

## 执行步骤

1. 在工作台顶部或右侧操作区补一个 `使用要点` 入口
2. 新增首次进入自动弹出的规则面板
3. 把规则内容收成少量高价值提示，不做长教程
4. 用本地存储记录“已看过”，避免每次进入都重复打断
5. 更新 README 和测试，重新跑前台相关校验

## 验收标准

- 首次进入工作台会自动出现规则提示层
- 用户关闭后，本地再次进入不会反复自动弹出
- 页面上始终能找到重新打开提示层的入口
- 提示内容明确覆盖产品图、模特图、主页参考、语言切换、脚本保护和提交边界
- 不影响现有 4 步向导切换
- 以下命令重新通过：
  - `node --check src/workspace.js`
  - `node --test tests/extension-shell.test.mjs`
  - `node --test tests/remake-core.test.mjs`
  - `git diff --check`

## 风险和需要注意的点

- 这层提示如果文案太长，会重新把页面变成“很多字”；所以只能保留最影响结果的规则
- 如果遮罩和 z-index 处理不好，会影响当前向导点击和滚动
- 本次只做本地“已看过”记录，不做跨设备同步

## 执行后的实际结果

- 已在工作台顶部补上 `使用要点` 入口
- 已在右侧操作区补上 `查看开始前要点` 入口
- 首次进入工作台会自动出现规则弹层
- 关闭后会把“已看过”写入本地存储，刷新页面后不会再次自动弹出
- 弹层内容已收成 6 条高价值规则，没有再堆成长教程
- 主流程 4 步切换保持不变，弹层关闭后直接回到第一步工作台

## 验证记录

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `node --test tests/remake-core.test.mjs`
- `git diff --check`
- 真实扩展页核查：
  - 首次刷新时弹层正常出现
  - 点击 `知道了，开始创建` 后回到第一步
  - 再次刷新后不再自动弹出
  - 右上角 `使用要点` 和右侧 `查看开始前要点` 入口均可见
