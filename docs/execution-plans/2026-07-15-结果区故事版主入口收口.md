# 2026-07-15 结果区故事版主入口收口

## 用户原始需求摘要

用户的目标不是局部功能，而是完整一条龙：

1. 选蒸馏模型
2. 传图
3. 自动识别卖点和场景
4. 生成提示词
5. 一键生成叙事图
6. 复制提示词和图片直接带走

## AI 对需求的理解

当前代码链路里，故事版能力本身已经有：

- 一键创建并自动等待结果
- 复制结果包
- 下载故事版图片

但主结果区还没有直接主入口，用户仍然要自己理解：

- 先去 `历史记录`
- 再切 `故事版图` 标签
- 再点生成或下载

这不符合“傻瓜式一条龙”。

## 当前假设

- 这轮不再扩新的生成能力。
- 当前最该补的是把故事版主动作提到结果区。
- 结果区按钮要根据真实状态自动切换成：
  - 生成故事版图
  - 继续等待故事版
  - 下载故事版图片

## 计划修改范围

- `src/workspace.html`
- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改蒸馏算法
- 不改抖音 / TikTok 提示词语言逻辑
- 不改蒸馏管理页结构
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`
- 不处理无关的 `src/workspace.css` 既有脏改

## 执行步骤

1. 先补测试，证明结果区存在故事版主入口。
2. 在结果区新增故事版主按钮，并按故事版真实状态切换文案和行为。
3. 同步 README 说明当前主流程。
4. 跑相关检查命令并完成 Git 闭环。

## 验收标准

- 生成完成后，结果区存在故事版主入口。
- 用户不需要先自己找 `故事版图` 标签，也能直接触发故事版主动作。
- 按不同状态自动切换成“生成 / 等待 / 下载”。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 本轮只收主入口，不改故事版底层任务逻辑。
- 结果区主按钮要与故事版详情页逻辑保持同一套状态判断，避免出现文案和真实行为不一致。

## 执行后的实际结果

- 已在结果区补上故事版主按钮。
- 按故事版真实状态自动切换为：
  - `生成故事版图`
  - `继续等待故事版`
  - `下载故事版图片`
- 点击结果区按钮会直接切到 `故事版图` 标签并执行对应主动作。
- README 已同步更新当前主流程说明。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
