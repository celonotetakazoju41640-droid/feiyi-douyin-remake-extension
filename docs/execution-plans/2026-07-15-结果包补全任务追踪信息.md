# 2026-07-15 结果包补全任务追踪信息

## 用户原始需求摘要

用户要的是一条龙主链路闭环，不只是本机当场能点通，还要把提示词、故事版、提交状态这些结果完整带走，方便跨电脑继续做或交给别人接力。

## AI 对需求的理解

代码审计发现当前 `复制结果包` 已经会带上：

- 视频提示词
- 关键帧提示词
- 候选版本
- 视频任务提示词
- 故事版结果

但视频任务段还缺少真正可追踪的交付信息：

- `taskId`
- `batchId`
- `sourceLinks`
- `extraRules`

这会导致结果包文本看起来完整，但真正提交后的追踪上下文不完整。

## 当前假设

- 本轮只补结果包文案里的任务追踪信息。
- 不改 UI，不改提交逻辑，不触发真实外部服务。

## 计划修改范围

- `src/remake-core.js`
- `tests/remake-core.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/workspace.js`
- 不改故事版生成逻辑
- 不改真实提交链路
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定结果包里必须包含视频任务追踪信息。
2. 修改 `buildDeliveryPackageText()`，把 `taskId / batchId / sourceLinks / extraRules` 打进结果包。
3. 同步 `README.md`。
4. 跑相关测试和差异检查。
5. 完成 Git 提交和推送。

## 验收标准

- `复制结果包` 产出的文本里，视频任务部分能看到 `taskId / batchId / sourceLinks / extraRules`。
- `node --test tests/remake-core.test.mjs`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`

## 风险和需要注意的事项

- 只补交付文本，不要误改实际任务提交逻辑。
- 结果包字段必须优先读现有任务数据，不能虚构状态。

## 执行后的实际结果

- 已补 `tests/remake-core.test.mjs`：新增断言，要求结果包文本必须带上视频任务 `taskId / batchId / sourceLinks / extraRules`，并继续保留故事版提示词。
- 已修改 `src/remake-core.js`：`buildDeliveryPackageText()` 现在会把视频任务的追踪信息一起带进复制文本。

## 验证命令或验证方式

- 已执行 `node --test tests/remake-core.test.mjs`
- 已执行 `node --test tests/extension-shell.test.mjs`
- 已执行 `git diff --check`
