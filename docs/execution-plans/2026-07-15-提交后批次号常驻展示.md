# 2026-07-15 提交后批次号常驻展示

## 用户原始需求摘要

用户要把主链路做完整，不只是能提交，还要能在提交后继续看懂当前项目到底进入了哪个批次。

## AI 对需求的理解

代码审计发现：

- 提交成功后，`batchId` 已经会回写进任务数据
- 但前台没有常驻展示这个批次号
- 用户只能在瞬时提示里看到一次，刷新后就丢线索

这会让“提交后继续追踪”这一环仍然不够顺。

## 当前假设

- 本轮只补批次号展示，不改提交逻辑本身。
- 不触发真实外部服务。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改故事版生成逻辑
- 不改真实生成链路
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定当前项目摘要应展示批次号。
2. 修改 `src/workspace.js`，把 `batchId` 常驻写进结果摘要。
3. 同步 `README.md`。
4. 跑检查命令并完成 Git 闭环。

## 验收标准

- 提交成功后，当前项目摘要区能直接看到批次号。
- 刷新或切回项目后，只要项目里已有 `batchId`，摘要仍能展示。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 不要只取内存态，要从项目数据里读，确保刷新后仍能展示。
- 不要把空批次号渲染成误导性文案。

## 执行后的实际结果

- 已修改 `src/workspace.js`：当前项目摘要现在会从已回写的任务数据里读取 `batchId` 并常驻展示。
- 已补测试，要求代码明确从 `batchVideoTasks` 里读取批次号，并渲染 `当前批次号：`。
- 已同步 `README.md`，补充批次号的前台展示规则。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
