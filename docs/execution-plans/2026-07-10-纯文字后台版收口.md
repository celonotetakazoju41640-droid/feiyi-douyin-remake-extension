# 2026-07-10 纯文字后台版收口

## 用户原始需求摘要

用户要求继续简化当前左侧菜单版前台：

- 保留左侧：
  - 生成
  - 历史记录
  - 蒸馏管理
- 右侧继续是内容区
- 但不要卡片感
- 不要很多胶囊标签
- 不要一堆说明文字
- 页面主要靠文字字号、字重和留白分层

## 我对本次任务的理解

这轮不是改功能，而是继续压视觉复杂度：

- 左侧菜单继续保留
- 右侧改成更像“纯文字后台表单”
- 减少大块背景、圆角边框和装饰性组件
- 缩减非必要说明文案

## 当前假设

- 继续复用现有左侧菜单结构和生成逻辑
- 只改前台 HTML/CSS 呈现，必要时只做少量显示适配
- 不改蒸馏算法、历史数据结构、API、计费和权限

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- `src/workspace.js`
- `src/remake-core.js`
- 真实外部服务调用
- 历史记录数据
- 蒸馏逻辑和后台协议

## 执行步骤

1. 精简左侧菜单文案和装饰
2. 精简右侧标题区和说明文案
3. 去掉明显卡片感、胶囊标签和大块圆角样式
4. 保留纯文字表单分组和必要分隔线
5. 更新 README 并重新校验

## 验收标准

- 左侧仍保留 `生成 / 历史记录 / 蒸馏管理`
- 右侧不再出现明显大卡片视觉
- 文案明显减少，主要靠标题和字段组织信息
- 页面仍可正常加载
- 以下命令重新通过：
  - `node --check src/workspace.js`
  - `node --test tests/extension-shell.test.mjs`
  - `node --test tests/remake-core.test.mjs`
  - `git diff --check`

## 风险和需要注意的点

- 如果把结构压得过头，可能导致分组关系不清楚，所以仍要保留最小分隔线
- 这轮只改视觉和文案，不顺手重排业务逻辑

## 执行后的实际结果

- 左侧菜单继续保留：
  - 生成
  - 历史记录
  - 蒸馏管理
- 右侧已经改成更偏纯文字后台表单
- 明显的大卡片感、圆角块和胶囊标签已大幅减少
- 顶部和主区说明文案已经继续精简
- 页面现在主要靠标题、字段、分隔线和留白组织层级

## 验证记录

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `node --test tests/remake-core.test.mjs`
- `git diff --check`
- 真实扩展页截图核查：
  - 左侧已收成纯文字菜单
  - 右侧主区已不再依赖大块卡片感
  - 页面主要靠文字和分隔线分层
