# 2026-07-13 历史记录工具区收口

## 用户原始需求摘要

用户确认继续，并要求严格按开发 skill 流程执行。本轮用户选择 `工具优先` 方案，继续收口 `历史记录` 页。

## 本次目标

只收 `历史记录` 页底部的工具区，让它更紧凑、更像后台工具抽屉，不改上面的主结果结构。

## 当前假设

- `当前结果 / 当前结果详情 / 最近生成` 三段主结构暂时不动
- 用户要的是工具区不要一展开就很重、很乱
- 镜头微调仍然保留，但应该后置成更弱一层

## 修改范围

- `src/workspace.html`
- `src/workspace.css`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改生成逻辑
- 不改历史数据结构
- 不改本地服务接口
- 不改蒸馏管理

## 执行步骤

1. 调整 `历史记录` 工具区结构
2. 把导入导出和服务状态放到第一层
3. 把镜头微调单独后置成更弱一层
4. 收紧工具区样式和说明文案
5. 更新 README
6. 跑检查

## 验收标准

- 工具区展开后先看到轻工具，不先被镜头编辑器压满
- 工具区整体高度和层级更像后台工具区
- 功能入口不丢
- `node --check src/workspace.js` 通过
- `git diff --check` 通过

## 执行后的实际结果

- 已把历史记录底部工具区拆成“常用工具 + 镜头微调”两层
- 已把导入导出和本地服务状态收进第一层轻工具区
- 已把镜头微调后置成更弱一层，减少一展开就很重的感觉
- 已同步更新 README 的历史记录说明
