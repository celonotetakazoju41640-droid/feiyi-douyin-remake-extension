# 2026-07-15 一键带走失败反馈补齐

## 用户原始需求摘要

用户要把“上传 -> 识别 -> 生成 -> 故事版 -> 复制提示词和图片带走”这条线真正收完整，前台不能只在顺利时好看，失败时也要说清楚。

## AI 对需求的理解

代码审计发现 `runDeliveryShortcut()` 目前只有 `try/finally`，没有 `catch`。

这意味着如果：

- 剪贴板复制失败
- 本地导出过程抛错
- 故事版图片下载链路抛错

用户只会看到按钮恢复，但不一定能拿到明确失败原因，状态摘要也可能停在“正在整理结果”。

这和“一键带走结果”应有的可理解性不一致。

## 当前假设

- 本轮只补异常反馈，不改一键带走的正常流程。
- 不触发真实外部服务。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改故事版生成逻辑
- 不改真实生成链路
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定一键带走失败时必须有明确反馈。
2. 修改 `src/workspace.js`，给 `runDeliveryShortcut()` 增加异常捕获和状态回退。
3. 同步 `README.md`。
4. 跑检查命令并完成 Git 闭环。

## 验收标准

- 一键带走任何一步抛错时，页面会明确提示失败原因。
- 一键带走失败时，不会把状态摘要永久卡在“正在整理结果”。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 不能把“部分成功”的场景误报成“完全失败”。
- 不能吞掉原始错误，否则排查会变难。

## 执行后的实际结果

- 已修改 `src/workspace.js`：给 `runDeliveryShortcut()` 增加 `catch`，一键带走任一步抛错时会同时更新状态摘要和顶部错误提示。
- 已补测试，要求代码明确包含 `一键带走失败：` 和 `结果整理失败：` 两类反馈口径。
- 已同步 `README.md`，补充一键带走失败时的前台表现。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
