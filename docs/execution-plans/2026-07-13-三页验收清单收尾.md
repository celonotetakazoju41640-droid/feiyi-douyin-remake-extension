# 2026-07-13 三页验收清单收尾

## 用户原始需求摘要

用户要求继续，并明确要求按验收清单执行，不再继续扩功能或大改结构。

## 本次目标

对 `生成 / 历史记录 / 蒸馏管理` 三页做一次完整页面验收，只修 5 个以内值得收尾的小问题。

## 当前假设

- 三页主结构已经基本收住
- 本轮重点是细碎的 UI、间距、文案和层级问题
- 不再新增功能，不再做大结构调整

## 修改范围

- `src/workspace.html`
- `src/workspace.css`
- `src/workspace.js`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不新增业务能力
- 不改生成逻辑
- 不改主页提炼逻辑
- 不改视频深蒸馏逻辑
- 不改本地服务接口

## 执行步骤

1. 生成三页本地预览图
2. 列出不超过 5 个待收尾问题
3. 只修这些问题
4. 同步 README
5. 跑检查并提交 Git

## 验收标准

- 三页没有明显的错位、过空、过挤或旧口径残留
- 修改范围保持在验收问题清单内
- `node --check src/workspace.js` 通过
- `git diff --check` 通过

## 执行后的实际结果

- 已按真实预览图收掉 4 个小问题：
  - 生成页补模型提示卡再压紧一层
  - 历史记录空状态补上更明确的默认承接内容
  - 蒸馏管理右侧深蒸馏按钮区进一步松开
  - README 里旧的 `6 条规则` 口径改成 `5 条规则`
- 已做第二轮三页截图复查，没有再发现值得继续动代码的明显问题
