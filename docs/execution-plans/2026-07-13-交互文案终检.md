# 2026-07-13 交互文案终检

## 用户原始需求摘要

用户确认继续，希望把当前这套前台再做一轮交互与文案终检，统一残留的状态字样、按钮文案和空状态提示。

## 我对本次任务的理解

当前功能已经基本齐了，本轮重点不是加新能力，而是把前台说法统一：

1. 空状态更一致
2. 状态字样更短
3. 按钮反馈更像同一套产品
4. 减少技术味和重复表达

## 当前假设

- 本轮只改前台文案，不改业务逻辑
- 用户更在意统一和简洁，不需要再补大段说明

## 计划修改范围

- `src/workspace.js`
- `src/workspace.html`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改本地服务
- 不改生成逻辑
- 不改蒸馏逻辑
- 不改历史结构和页面布局

## 执行步骤

1. 统一空状态文案
2. 统一按钮反馈文案
3. 统一主页蒸馏状态字样
4. 统一本地服务状态字样
5. 更新 README 和计划文档
6. 跑检查

## 验收标准

- 生成 / 历史记录 / 蒸馏管理关键提示更短、更一致
- `node --check src/workspace.js` 通过
- `git diff --check` 通过

## 风险和需要用户确认的事项

- 本轮只改说法，不改功能

## 执行后的实际结果

- 已统一一批核心状态字样：
  - `等待生成` -> `未生成`
  - `系统自动` -> `自动判断`
  - `手动模式` -> `手动填写`
  - `待重扫` -> `需要重扫`
  - `扫描中 / 扫描失败` -> `提炼中 / 提炼失败`
  - `可转发 / 队列模式` -> `可提交 / 仅本地`
- 已统一一批空状态和提示语：
  - 当前结果空状态
  - 最近记录空状态
  - 模板引导卡
  - 视频深蒸馏空状态
- 已把生成页里几处偏技术或偏重的说法收短：
  - `先选择蒸馏模型，再上传产品图` -> `先选模型，再上传产品图`
  - `产品图必填，模特图可选` -> `上传产品图后会自动拆卖点`
  - `可选蒸馏模型` -> `可用模型`
- README 已同步说明前台状态字样已统一收口

## 验证方式

- 打开三大栏目检查文案
- 运行：
  - `node --check src/workspace.js`
  - `git diff --check`
