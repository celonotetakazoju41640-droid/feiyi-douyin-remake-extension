# 2026-07-15 向导查看步骤不误报缺图

## 用户原始需求摘要

用户要的是主链路前后口径一致，恢复历史项目后只是查看结构，不能还被错误提示成“没上传商品图”。

## AI 对需求的理解

代码审计发现：

- 向导第 3 步 / 第 4 步只是查看结构时
- 仍使用当前文件输入是否为空来判断

这会导致历史恢复后的项目，即使项目里已经记录过商品图数量，也会被误提示：

- `还没上传商品图。你可以先看后面的结构，但正式生成前需要先补商品图。`

## 当前假设

- 本轮只修向导查看步骤的提示口径。
- 不改 fresh upload 的重新生成门槛。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改生成逻辑本身
- 不改故事版逻辑
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定向导查看步骤会参考已知商品图数量。
2. 修改 `setWizardStep()`，让第 3 步 / 第 4 步的提示口径与已知项目状态一致。
3. 同步 `README.md`。
4. 跑检查命令并完成 Git 闭环。

## 验收标准

- 历史恢复后的项目，在已知曾上传过商品图时，切到第 3 步 / 第 4 步不会再误报缺图。
- 真正重新生成时，fresh upload 门槛仍然保留。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`

## 风险和需要注意的事项

- 不能因为修提示口径，把重新生成门槛放松。
- 只调整查看步骤提示，不改实际生成条件。

## 执行后的实际结果

- 已补 `tests/extension-shell.test.mjs`：要求 `setWizardStep()` 使用 `getKnownProductImageCount()`。
- 已修改 `src/workspace.js`：恢复项目后，向导查看后续步骤会参考已知商品图数量，不再误报缺图。

## 验证命令或验证方式

- 已执行 `node --check src/workspace.js`
- 已执行 `node --test tests/extension-shell.test.mjs`
- 已执行 `git diff --check`
