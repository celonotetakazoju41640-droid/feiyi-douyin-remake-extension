# 2026-07-08 TikTok 主页统计分行口径修复执行计划

## 用户原始需求摘要

继续完善“飞蚁抖音复刻”Chrome 扩展，把 TikTok / 抖音公开主页蒸馏继续做稳，发现可用的能力就直接补完并提交。

## AI 对需求的理解

这轮不是扩功能范围，而是补一个真实主页里已经暴露出来的解析缺口：TikTok 主页粉丝数和获赞数有时会出现“数字一行、标签一行”的展示方式，当前 helper 会把这类账号体量误判成 0，需要修到公开主页蒸馏链路里可直接复用。

## 当前假设

- 主项目目录是`/Users/da/Desktop/飞蚁抖音复刻-Chrome扩展`。
- 用户已明确允许继续执行，不需要再次等待口头确认。
- 这轮只修公开主页统计文本解析，不扩到登录态抓取、私有内容或真实视频生成。
- 当前已有失败测试可以作为本轮 TDD 的红灯起点。

## 计划修改范围

- `src/remake-core.js`
- `tests/remake-core.test.mjs`
- `README.md`
- `ROADMAP.md`
- `docs/execution-plans/2026-07-08-TikTok主页统计分行口径修复.md`

## 明确不修改的范围

- 不改 UI 布局和样式。
- 不改 `src/workspace.js` 的主流程结构。
- 不接真实 API、不保存密钥。
- 不扩大到抖音深翻、登录态抓取或私有内容抓取。

## 执行步骤

1. 复核失败测试和现有 helper 的真实行为。
2. 在`src/remake-core.js`里补“同一行”和“分行标签”两种统计口径兼容。
3. 同步更新 README 和路线图，写清当前 TikTok 主页统计兜底能力。
4. 跑语法、测试和 diff 检查。
5. 走 Git 提交和推送闭环。

## 验收标准

- `parseTikTokProfileStatsText`同时支持以下格式：
  - `27.1M followers`
  - `27.1M` 下一行 `粉丝`
  - `1.1B likes`
  - `1.1B` 下一行 `赞`
- `node --check src/remake-core.js`通过。
- `node --check src/workspace.js`通过。
- `node --test tests/remake-core.test.mjs`通过。
- `git diff --check`通过。
- Git 有新的相关提交并成功推到远端。

## 风险和需要注意的事项

- 这轮不触发真实外部服务，不产生真实视频成本。
- 这里只能修文本解析口径，不能承诺 TikTok 公开页所有字段都百分百稳定。
- 如果验证时发现问题已经扩大到抓取主流程，而不是 helper 本身，会先停下来重新说明范围。

## 执行后的实际结果

- 已在`src/remake-core.js`补上 TikTok 主页统计的分行口径兼容：
  - 继续支持“数字和标签同一行”
  - 新增支持“上一行是数字、当前行是标签”
  - 同时兼容 `followers / 粉丝 / likes / 获赞 / 点赞 / 赞`
- 真实缺口对应的失败测试已转绿，`NBA` 这类主页统计不再把粉丝和获赞误判成 0。
- 已同步更新`README.md`和`ROADMAP.md`，把当前 TikTok 主页统计兜底能力写清楚。
- 当前改动没有扩大到 UI、服务端或真实生成链路。

## 验证命令或验证方式

- `node --check src/remake-core.js`
- `node --check src/workspace.js`
- `node --test tests/remake-core.test.mjs`
- `git diff --check`
