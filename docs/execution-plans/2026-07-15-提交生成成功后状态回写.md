# 2026-07-15 提交生成成功后状态回写

## 用户原始需求摘要

用户要把主链路真正做成“生成 -> 提交 -> 继续处理”一条线，前台状态必须说真话。

## AI 对需求的理解

代码审计发现当前 `提交生成` 成功后，只会提示：

- 已发送到本地服务
- 返回批次号

但不会把项目里的 `batchVideoTasks.status` 回写成已提交状态。

而历史卡片状态 `已提交 / 可提交` 正是靠这个字段判断的，所以用户提交成功后，卡片仍可能继续显示 `可提交`。

## 当前假设

- 本轮只补提交成功后的状态回写和前台状态同步。
- 不触发真实外部服务。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改故事版生成逻辑
- 不改真实生成链路
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定提交成功后必须回写任务状态。
2. 修改 `src/workspace.js`，提交成功后更新 `batchVideoTasks` 状态并刷新历史区。
3. 同步 `README.md`。
4. 跑检查命令并完成 Git 闭环。

## 验收标准

- 提交生成成功后，历史卡片状态会变成 `已提交`。
- 当前项目摘要里的状态标记也会同步到 `已提交`。
- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 不能把失败提交误写成已提交。
- 不能覆盖故事版或其他已有状态字段。

## 执行后的实际结果

- 已修改 `src/workspace.js`：提交生成成功后，会把 `batchVideoTasks.status` 回写成已提交状态，并刷新当前项目和历史卡片。
- 已补测试，要求代码明确回写 `task.status / task.batchId`，并把当前项目状态徽标绑定到历史状态判断。
- 已同步 `README.md`，补充提交成功后的前台状态变化。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
