# 2026-07-14 历史记录继续减法重排

## 用户原始需求摘要

用户看完上一轮历史记录重构后的实际界面，认为页面还是乱，要求继续做减法，不要让多个内容块同时抢注意力。

## 我对本次任务的理解

本轮继续只改 `历史记录` 页，不改底层逻辑，重点是把结构继续收窄：

1. 首屏只留项目库
2. 用户先选项目
3. 选中后再展开工作台

这样避免 `项目库 / 当前项目 / 项目详情` 三块同时占住首屏。

## 当前假设

- 上一轮的主要问题不是样式不够，而是主结构仍然过重
- 当前项目工作台可以在选中项目后再显示，不必默认常驻
- 导入、本地服务、镜头微调继续维持折叠，不进入首屏

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改生成逻辑
- 不改提交生成链路
- 不改蒸馏管理逻辑
- 不改生成页
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 把历史记录首屏压成项目卡片库
2. 把当前项目工作台改成“选中后出现”
3. 删掉多余的统计和解释文案
4. 保留主操作与详情双栏，但默认后置
5. 更新测试、README 和计划文档
6. 跑检查并完成 Git 提交上传

## 验收标准

- 首屏只保留项目卡片库，不再有三块主内容同时争抢注意力
- 选中项目后才出现当前项目工作台
- `node --check src/workspace.js` 通过
- `node --test tests/extension-shell.test.mjs` 通过
- `git diff --check` 通过

## 风险和需要用户确认的事项

- 本轮继续调整历史记录页结构，但不改结果数据本身
- `src/workspace.css` 里仍有不属于本轮的未提交改动，提交时只暂存本轮相关内容

## 执行后的实际结果

- 已把历史记录首屏继续压成只保留项目卡片库
- 已把详情工作台改成有项目时才显示，避免首屏同时出现三块主内容
- 已删掉一部分重复统计和解释文案
- 已同步 README 和测试断言
- 本轮未改生成逻辑、提交生成链路和蒸馏逻辑

## 验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
