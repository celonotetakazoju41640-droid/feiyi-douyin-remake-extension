# 2026-07-15 历史记录缩略图与时间

## 用户原始需求摘要

用户明确指出：历史记录项目卡应该展示图片缩略图，并且要把生成时间标清楚。

## 我对本次任务的理解

本轮继续只改 `历史记录` 页，不改底层生成逻辑，目标是补足两项对用户判断最直接的信息：

1. 项目缩略图
2. 生成时间

## 当前假设

- 当前项目记录里已有 `createdAt`，可以直接用于时间展示
- 当前项目记录里没有可长期复用的缩略图字段，需要在生成时补存
- 新增字段只挂在本地项目记录上，不影响现有核心包结构

## 计划修改范围

- `src/workspace.js`
- `src/workspace.css`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改生成逻辑本身
- 不改提交生成链路
- 不改蒸馏管理逻辑
- 不改生成页结构
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 生成项目时把首张商品图转成可持久化缩略图
2. 历史记录卡片展示缩略图
3. 历史记录卡片展示生成时间
4. 为旧记录保留无图回退样式
5. 更新测试、README 和计划文档
6. 跑检查并完成 Git 提交上传

## 验收标准

- 新生成的项目卡片能直接看到商品图缩略图
- 卡片能看到清晰的生成时间
- 旧记录无缩略图时不会报错，会自动走回退样式
- `node --check src/workspace.js` 通过
- `node --test tests/extension-shell.test.mjs` 通过
- `git diff --check` 通过

## 风险和需要用户确认的事项

- 本轮会给本地历史项目记录增加缩略图字段，但不改核心生成结果结构
- `src/workspace.css` 里仍有不属于本轮的未提交改动，提交时只暂存本轮相关内容

## 执行后的实际结果

- 已在生成项目时把首张商品图转成可持久化缩略图并写入本地项目记录
- 已在历史记录卡片展示缩略图、生成时间和轻量状态
- 旧记录没有缩略图时会自动走回退样式，不影响页面使用
- 已同步 README 和测试断言
- 本轮未改生成逻辑、提交生成链路和蒸馏逻辑

## 验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
