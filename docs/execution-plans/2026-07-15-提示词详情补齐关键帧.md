# 2026-07-15 提示词详情补齐关键帧

## 用户原始需求摘要

用户要把“选择蒸馏模型 -> 一键传图 -> AI 自动识别产品卖点场景 -> 生成提示词 -> 一键点击生成故事版 -> 复制提示词和图片带走”这条链路真正做完整，细节要考虑到位。

## AI 对需求的理解

当前历史详情页里有 `提示词` 标签，但这里只展示了视频提示词，没有把关键帧提示词一起展示出来。

这和当前真实交付物存在落差：

- `复制结果包` 会带关键帧提示词
- 导出的 Markdown / 文本包也会带关键帧提示词
- 但用户在前台详情里却看不到关键帧这一层

对于“生成提示词 -> 复制提示词和图片带走”这一步，前台检查入口不完整。

## 当前假设

- 本轮不改提示词生成算法，只补齐详情页展示。
- `提示词` 标签应该同时展示：
  - 视频提示词
  - 关键帧提示词
- 不需要新增新标签页，直接在现有 `提示词` 详情里补齐即可。

## 计划修改范围

- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`
- 不改批量任务导出逻辑
- 不改故事版生成逻辑

## 执行步骤

1. 先补测试，锁定详情页 `提示词` 标签会同时展示视频提示词和关键帧提示词。
2. 最小修改 `src/workspace.js` 的详情页提示词内容组装。
3. 同步 `README.md`。
4. 跑验证。
5. 只提交本轮相关文件并推送。

## 验收标准

- 详情页 `提示词` 标签里能同时看到视频提示词和关键帧提示词。
- 不影响导出包、复制结果包、故事版和批量任务现有逻辑。
- `node --test tests/extension-shell.test.mjs`
- `node --check src/workspace.js`
- `git diff --check`
- `git diff --cached --check`

## 风险和需要注意的事项

- 不能只把关键帧拼进去却让原来的视频提示词消失。
- 不能把详情页文本结构做乱，影响复制阅读。

## 执行后的实际结果

- 已补一条壳测试，锁定详情页 `提示词` 标签会同时展示视频提示词和关键帧提示词。
- 已修改 `src/workspace.js`：当前项目详情里的 `提示词` 标签会把视频提示词和关键帧提示词分段一起展示出来。
- 已同步 `README.md`，补充前台详情页现在可以直接检查两层提示词，不必先导出或复制结果包。

## 验证命令或验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
