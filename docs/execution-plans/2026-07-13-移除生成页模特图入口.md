# 2026-07-13 移除生成页模特图入口

## 用户原始需求摘要

用户要求继续完善当前前台，延续上一轮收口方向，把生成页里残留的模特图入口继续处理掉。

## 我对本次任务的理解

这轮不是加新功能，而是把普通用户主流程进一步简化：

1. 生成页只保留产品图上传
2. 删除模特图上传入口和相关状态
3. 同步删掉对应引导文案和说明文档

## 当前假设

- 模特图没有进入当前生成核心，只是前台可选输入
- 删除这个入口不会影响提示词生成、历史记录和蒸馏管理
- 用户要的是更简单的 C 端主流程

## 计划修改范围

- `src/workspace.html`
- `src/workspace.js`
- `src/workspace.css`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改生成核心逻辑
- 不改蒸馏管理和深蒸馏流程
- 不改本地服务
- 不改历史记录结构

## 执行步骤

1. 删除生成页模特图上传区和状态展示
2. 删除对应前台事件、预览和文案逻辑
3. 删除只服务模特图的样式
4. 更新 README 和本计划文档
5. 跑检查

## 验收标准

- 生成页主流程只保留产品图上传
- 页面上不再出现模特图上传入口和对应状态
- `node --check src/workspace.js` 通过
- `git diff --check` 通过

## 风险和需要用户确认的事项

- 本轮会让生成页变得更简单，但等于明确取消普通用户主流程里的模特图入口

## 执行后的实际结果

- 已删除生成页里的模特图上传入口
- 已删除对应素材状态、前台事件和预览逻辑
- 已把开始前先看从 6 条收成 5 条
- 已同步更新 README 的生成页说明，主流程只保留产品图上传

## 验证方式

- 打开生成页确认只剩产品图上传
- 运行：
  - `node --check src/workspace.js`
  - `git diff --check`
