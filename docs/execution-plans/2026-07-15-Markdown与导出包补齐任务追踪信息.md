# 2026-07-15 Markdown 与导出包补齐任务追踪信息

## 用户原始需求摘要

用户要的是整条链路做完以后，不管是复制结果、下载 Markdown，还是整包导出，都能把后续接力需要的信息一起带走。

## AI 对需求的理解

上一轮已经补了 `复制结果包` 里的视频任务追踪信息，但继续审计发现：

- `下载 Markdown`
- 导出包里的 `06-批量视频任务.md`

这两个出口还没有同步带上：

- `batchId`
- `sourceLinks`
- `extraRules`

这会造成结果出口之间字段不一致，影响跨电脑继续追任务或交给别人接力。

## 当前假设

- 本轮只补 Markdown 与导出包里的任务追踪字段。
- 不改 UI，不改真实提交逻辑，不触发真实外部服务。

## 计划修改范围

- `src/remake-core.js`
- `tests/remake-core.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改 `src/workspace.js`
- 不改故事版生成逻辑
- 不碰 `src/workspace.css`
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 补测试，锁定 Markdown 和导出包里的任务追踪字段。
2. 修改 `buildMarkdownFromPackage()` 和 `buildExportBundle()` 内部的任务 Markdown 生成逻辑。
3. 同步 `README.md`。
4. 跑测试与差异检查。
5. 完成 Git 提交和推送。

## 验收标准

- `下载 Markdown` 里的批量视频任务段可看到 `任务编号 / 批次号 / 参考链接 / 附加要求`。
- 导出包里的 `06-批量视频任务.md` 可看到同样字段。
- `node --test tests/remake-core.test.mjs`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`

## 风险和需要注意的事项

- 只补导出文本字段，不误改真实任务数据。
- 三个出口的字段口径要保持一致，避免一处完整、一处残缺。

## 执行后的实际结果

- 已补 `tests/remake-core.test.mjs`：新增断言，要求 Markdown 和导出包任务 Markdown 都保留 `任务编号 / 批次号 / 参考链接 / 附加要求`。
- 已修改 `src/remake-core.js`：`buildMarkdownFromPackage()` 和导出包里的 `tasksMarkdown` 现在已同步带上这些字段。

## 验证命令或验证方式

- 已执行 `node --test tests/remake-core.test.mjs`
- 已执行 `node --test tests/extension-shell.test.mjs`
- 已执行 `git diff --check`
