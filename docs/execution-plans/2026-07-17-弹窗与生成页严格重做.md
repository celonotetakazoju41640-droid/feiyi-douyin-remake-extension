# 2026-07-17 弹窗与生成页严格重做

## 用户原始需求摘要
按专业 skill 的执行方式，重做 Chrome 扩展的入口弹窗和生成页，不再凭感觉补 UI。目标是把“选蒸馏模型 -> 传商品图 -> AI 自动识别卖点和场景 -> 自动补提示词 -> 生成叙事图 -> 复制提示词和图片带走”收成一条真正傻瓜式链路。

## 这次我对需求的理解
这次不是小修，而是把两层入口统一：
- 扩展弹窗必须和工作台说同一套话，不能再停留在“参考视频拆解 / Seedance 返工”的旧口径。
- 工作台生成页必须压成真正单链路，不能再让用户一眼看到多个半成品区块、空状态大白板和次流程抢主流程。

## 当前假设
- 真实目标仓库仍是 `/Users/da/Desktop/飞蚁抖音复刻-Chrome扩展`。
- 业务主链路没有变，问题主要在入口口径、页面层级和状态反馈。
- 当前工作区里 `src/workspace.css` 已存在与本次无关的未提交改动，本次提交不能混进去。
- 这次不改商业规则、不接新服务、不改资源消耗逻辑。

## 计划修改范围
- `src/popup.html`
- `src/popup.js`
- `src/popup.css`
- `src/workspace.html`
- `src/workspace.js`
- `src/workspace.css`（只提交本轮相关样式）
- `tests/extension-shell.test.mjs`
- `README.md`
- `docs/execution-plans/2026-07-17-弹窗与生成页严格重做.md`

## 明确不修改的范围
- 不改 `蒸馏管理` 和 `历史记录` 的整页结构，只处理和主链路直接相关的承接入口。
- 不处理无关未跟踪文件：`scripts/runtime/`、`src/workspace-flow-test.html`。
- 不把 `src/workspace.css` 里现有深蒸馏样式改动一起提交。
- 不改真实外部服务、计费、上传、支付或部署逻辑。

## 执行步骤
1. 先补测试，明确弹窗必须改成什么口径、生成页必须压成什么结构。
2. 先跑测试确认失败，锁定这轮目标不是旧实现已经满足的行为。
3. 重做弹窗：首页话术、主动作、最近项目承接全部改成当前真实链路。
4. 重做生成页：压缩成单列主流程，弱化旁支，清掉视觉空块和旧提示。
5. 同步 README 里的用户使用口径。
6. 跑测试、语法检查、diff 检查。
7. 只提交本轮相关文件，保留无关未提交改动。

## 验收标准
- 弹窗首页不再出现“参考视频拆解 / Seedance 返工 / 上传参考视频”这类旧口径。
- 弹窗首页能直接说明当前真实链路，并把主入口聚焦到“打开生成工作台”。
- 生成页首屏不再出现右侧大片空块，不再让多个半流程并列抢注意力。
- 用户第一眼能明确看到：先选模型、再传图、等 AI 自动识别和补草稿、最后点主按钮带走结果。
- 生成完成后，生成页自身能给复制提示词和去结果区的清晰承接。
- `node --test tests/extension-shell.test.mjs` 通过。
- `node --check src/popup.js`、`node --check src/workspace.js`、`node --check src/remake-core.js` 通过。
- `git diff --check` 通过。

## 风险和注意事项
- `src/workspace.css` 当前已有其他未提交改动，本次只能挑本轮相关样式入提交。
- 这次不触发真实外部服务，不产生新的真实成本。

## 执行后的实际结果
- 弹窗首页已重做成当前真实主链路入口，不再保留“参考短视频拆解 / Seedance 返工 / 上传参考视频”旧口径。
- 生成页首屏已改成单列 4 步主链路：选模型、传商品图、等系统自动往下走、生成并带走结果。
- 原来右侧那块空的辅助说明板已移除，自动识别和结果承接都收回主链路内部。
- 生成完成后，生成页内部继续保留复制提示词草稿、继续一键带走、去历史记录看完整结果这几个承接口。
- README 和测试已同步更新。

## 验证命令或验证方式
- `node --test tests/extension-shell.test.mjs`
- `node --check src/popup.js`
- `node --check src/workspace.js`
- `node --check src/remake-core.js`
- `git diff --check`
