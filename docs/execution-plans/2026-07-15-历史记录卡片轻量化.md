# 2026-07-15 历史记录卡片轻量化

## 用户原始需求摘要

用户查看当前历史记录首屏后，认为界面还是乱，主要问题是每张项目卡过大、过长、信息太多。希望继续简化，让首屏只承担“选项目”。

## 我对本次任务的理解

本轮继续只改 `历史记录` 页，不改底层逻辑，重点是把项目卡片收成轻量列表：

1. 每张卡只保留最少必要信息
2. 首屏先负责选项目
3. 长内容全部后置到详情工作台

## 当前假设

- 项目卡片当前最大问题是高度和长文本，不是缺信息
- 模板名、项目名、少量 badge 和一条短摘要足够支持首屏选择
- 工作台和折叠工具区继续沿用现有结构，不在本轮重做

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `src/workspace.js`
- `tests/extension-shell.test.mjs`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- 不改生成逻辑
- 不改提交生成链路
- 不改蒸馏管理逻辑
- 不改生成页
- 不碰 `scripts/runtime/`
- 不碰 `src/workspace-flow-test.html`

## 执行步骤

1. 收短历史记录页顶部说明
2. 把项目卡改成轻量列表卡
3. 删掉卡片内长段正文和大面积无效预览
4. 保留主按钮与少量 badge
5. 更新测试、README 和计划文档
6. 跑检查并完成 Git 提交上传

## 验收标准

- 项目卡首屏明显更轻，不再像三篇并排长文
- 用户可以快速扫一眼后点进详情
- `node --check src/workspace.js` 通过
- `node --test tests/extension-shell.test.mjs` 通过
- `git diff --check` 通过

## 风险和需要用户确认的事项

- 本轮继续调整历史记录页前台结构，但不改结果数据本身
- `src/workspace.css` 里仍有不属于本轮的未提交改动，提交时只暂存本轮相关内容

## 执行后的实际结果

- 已把项目卡改成轻量列表卡，只保留模板名、商品名、短摘要、少量 badge 和主按钮
- 已删掉卡片里的大面积预览区和长段正文
- 已保留首屏“先选项目”，把长内容继续后置到详情工作台
- 已同步 README 和测试断言
- 本轮未改生成逻辑、提交生成链路和蒸馏逻辑

## 验证方式

- `node --check src/workspace.js`
- `node --test tests/extension-shell.test.mjs`
- `git diff --check`
