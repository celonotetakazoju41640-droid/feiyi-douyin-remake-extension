# 2026-07-10 首页上传格与模板下拉收口

## 用户原始需求摘要

用户要求：

- 产品图上传后直接显示到左侧示例格上
- “示例”这个字不用再显示
- 把“模特图可选”这句删掉
- 蒸馏模型下拉里不要再显示 `· @账号`

## 我对本次任务的理解

这轮只处理首页生成区的 3 个展示细节，不改功能范围。

## 当前假设

- 用户只是要优化首页可见文案和预览表现，不是要删掉模特图上传能力
- 这轮只改 `src/workspace.html`、`src/workspace.css`、`src/workspace.js`
- 模板真实数据结构不变，只改下拉显示文案

## 计划修改范围

- `src/workspace.html`
- `src/workspace.css`
- `src/workspace.js`
- `README.md`
- 本执行计划文档

## 明确不修改的范围

- `src/remake-core.js`
- 历史记录
- 蒸馏管理
- 生成逻辑

## 执行步骤

1. 去掉产品图预览格里的“示例”字样
2. 确保产品图上传后直接占用左侧预览格
3. 删除模特图上传格里的“模特图可选”文案
4. 修改模板下拉显示，只保留模板名
5. 抓真实页面截图核对

## 验收标准

- 首页不再显示“示例”字样
- 产品图上传后预览直接在左侧格里显示
- 模特图上传格不再显示“模特图可选”
- 蒸馏模型下拉只显示模板名

## 执行后的实际结果

- 产品图预览格已经去掉“示例”字样
- 产品图上传后会直接占用左侧预览格
- 模特图上传格里的“模特图可选”文案已删除
- 蒸馏模型下拉当前只显示模板名，不再带 `· @账号`

## 验证记录

- 真实页面截图核对：
  - 首页上传格已无“示例”字样
  - 模特图上传格已无“模特图可选”
